# types

Shared TypeScript types (product, category, quote, etc.), generally
including generated Supabase database types once tables exist. Empty
placeholder for now — no tables have been created yet.
